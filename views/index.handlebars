<h1>Eat-Da-Burger!</h1>

<main>
  <div class="container">
    <div class="row">
      <div class="col-6">
        {{#each burgers}}
        {{#unless this.devoured}}
        <div>
          <form >
            <button type="submit" class='notEaten' data="{{this.id}}"> {{this.burger_name}}</button>
          </form>
        </div>
        {{/unless}}
        {{/each}}
      </div>

    </div>
      <div class="col-6">
        {{#each burgers}}
        {{#if this.devoured}}
        <div class='burgerEaten'>
          <div>
            {{this.burger_name}}
          </div>
        </div>
        {{/if}}
        {{/each}}
      </div>
  </div>
</main>

<h2>Add A Burger Option</h2>

<div class="container">

  <div class="row">
    <div class="col-12">
      <form action="/burgers" method="POST">
        <input type="text" id="name" name="burger_name" placeholder="Declare your burger">
        <button type="submit" class="addBurgerButton">Engage Burger!</button>
      </form>
    </div>
  </div>
</div>

 <script>
    $(".addBurgerButton").on("click", function(){
      event.preventDefault();
       let newBurger={burgerName: $('#name').val()}
        $.post("/api/burger",newBurger, (res)=>console.log(res) )
    })
    $.get
    $("#newBurger").append()

   $(".notEaten").on("click", function() {
     console.log(this)
     console.log(this.data)
     console.log($(this))
      event.preventDefault();
      $.post("/api/burger/:id", "", (res)=>console.log(res) )
    })
    </script>
     {{!-- All I need to do really is get the data-id from my uneaten burger buttons so i can send it to my update functions, but I can't seem to get it working --}}

    